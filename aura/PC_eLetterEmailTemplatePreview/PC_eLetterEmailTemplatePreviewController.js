/**
 * Created by havalakki on 4/9/2019.
 */
({
    doInit: function(component, event, helper){

        helper.setNamespace(component);
        helper.previewEletter(component, event, helper);
    }

})